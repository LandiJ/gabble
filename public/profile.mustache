<!DOCTYPE html>
<html lang="en">

<head>
    <title></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="main.css" rel="stylesheet">
</head>

<body>


    <div class="profile_header">


        <h1 class="gabble_profile_header">Gabble!!</h1>


        <div class="user">

            {{#user}}
            <div class="dropdown">
                <button class="dropbtn">Logged in as {{user.name}}</button>
                <div class="dropdown-content">
                    <a href="./login">Login</a> <br>
                    <a href="./in">Profile</a><br>
                    <a href="./signup">Signup</a><br>
                    <a href="./homepage">Homepage</a>
                    <form action="/logout" method="post">

                        <input class="submit" type="submit" value="Logout">

                    </form>
                    </a>
                </div>
            </div>
            {{/user}} {{^user}}

            <h3>No user logged in</h3>
            {{/user}}
        </div>
    </div>
    {{#user}}
    <h4 class="name">{{user.name}}</h4>
    <p class="post">{{user.bio}}</p>
    {{/user}}

    <form action="/post" method="post">
        <label for="">What's New?</label>
        <textarea rows="4" cols="50" name="body">
Enter text here...</textarea>
        <input type="submit" value="Post">

    </form>

    {{#posts}}
    <div class="post" <span class="name">{{user.name}}</span><span class="body">{{body}}</span><br>
        <form action="/delete-post/{{id}}" method="post">
            <input type="Submit" value="Delete This Post">
        </form>
    </div>

    {{/posts}}




</body>

</html>